class Rational(val numerator: Int, val denominator: Int)

fun main(args: Array<String>) {
    val a = readLine()
    val b = readLine()

  
    val sum:Int = Integer.parseInt(a) + Integer.parseInt(b)

    println(sum.toString())
}
